{
  "name": "salary-manager",
  "version": "1.0.0",
  "private": true,
  "description": "A simple salary and deductions tracker with SQLite persistence.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
